# fnm

## Install

```bash
brew install fnm
```

**Configuration**

Add the follow command in your profile `~/.bashrc` o `~/.zshrc`

```bash
eval "$(fnm env --use-on-cd)"
```

**Reload or apply changes**

```bash
source ~/.bashrc
```

o

```bash
source ~/.zshrc
```
